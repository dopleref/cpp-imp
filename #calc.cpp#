#include <iostream>
#include <sstream>
#include <string>
#include <regex>

int main() {
    /*
    std::string is;
    std::getline(std::cin, is);
    std::istringstream iss(is);
    
    std::string s2;
    while (iss >> s2) {
        std::cout << s2 << " ";
    }
    std::cout << std::endl;

    std::cout << is << std::endl;


    */
    return 0;
}
